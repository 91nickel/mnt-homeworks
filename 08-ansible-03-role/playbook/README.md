ѕлейбук устанавливает Java, Elastic Search и Kibana в докер-контейнере foroffice_ubuntus_1  

¬се выполн€етс€ в три пле€, каждый компонент устанавливаетс€ по очереди

## JAVA
1. ќпредел€етс€ директори€, в которой будет лежать java. ќна зависит от версии, котора€ определ€етс€ переменной
2. — локальной машины загружаетс€ предварительно скачанный архив. ”казываетс€ путь к файлу на локальной машине и путь назначени€
3. —оздание директории, путь к которой определили в пункте 1. ”казываетс€ назначение (state) и путь (src)
4. »звлечение Java в подготовленную директорию.  
**src** - указываетс€ тот путь, куда мы копировали архив,
**copy** - говорит о том, надо ли копировать файл с локальной машины (в данном случае нет)  
**dest** - рабоча€ директори€,  
**creates** - не будет ничего делать если указанный в нем путь существует, 
**--strip-components=1** - это параметр функции tar, извлекать в текущую папку
5. —оздаем переменные окружени€ домашнюю директорию Java и добавл€ем в PATH

## Elastic Search
1. ѕолучаем файл архива с удаленного хоста  
**url** - откуда качать (зависит от версии из переменной)  
**dest** - куда сохран€ем (тоже в название файла подт€гиваетс€ верси€)
**mode** - права, которые будут на файле
**timeout** - таймаут запроса
**force** - замен€ть / не замен€ть
2. —оздаетс€ директори€ под ES (зависит от версии), параметры те же, что в пред. шаге
3. –азархивируем точно так же как и в пред. шаге
4. ”станавливаем переменные окружени€ дл€ ES

## Kibana
1. ѕолучаем файл архива с удаленного хоста 
2. —оздаетс€ директори€ под Kibana (зависит от версии), параметры те же, что в пред. шаге
3. –азархивируем точно так же как и в пред. шаге
4. ”станавливаем переменные окружени€ дл€ Kibana
